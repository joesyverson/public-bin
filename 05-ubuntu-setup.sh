# the following is a script that sets up my personal machine as I like it
# most of the packages are CLI tools

# V1 will simply insall everything and list packages to be installed
	# OPTIONS:
		# -l, --list
		# -h, --help
# V2 will take arguments to remove packages from the default list
	# OPTIONS
		# -n, --not
